# React + TypeScript + Vite

For the assignment I prioritized pixel-perfect alignment with the Figma design, therefore I used Tailwind arbitrary values. In a production setting, I'd extract these into theme tokens or a shared design system.